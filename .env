VITE_LIVE_SERVER_URL="http://localhost:8080"

# Path the app is served from — used for the PWA manifest/icon URLs and the
# manifest's start_url/scope. Dev server serves from the domain root.
VITE_BASE_PATH=/
