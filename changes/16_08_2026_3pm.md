# BillSplitter v3 Rework — Progress Log (2026-08-16 3pm plan)

Tracks execution of `/Users/satyajeetnigade/.claude/plans/temporal-roaming-sky.md`.

## Phase 0 — Groundwork: presence tracker, permission model, creator identity ✅ done 2026-08-16
- [x] `server/internal/presence/presence.go` — Tracker (Touch/IsAvailable/ListOnline) + sweep
- [x] Go unit tests for presence.Tracker
- [x] Migration `0005_permission_and_identity.sql` (permission_mode, creator_person_id)
- [x] Store layer threads new columns (insert session w/ NULL creator_person_id, insert people, backfill — FK ordering)
- [x] `POST /api/sessions` accepts permissionMode + creatorPersonId (must reference a person in the request)
- [x] Join handler: reject creator-identity claim (403), reject active-identity reclaim (409 via presence.IsAvailable), touch presence on success
- [x] Go tests for join-rejection cases (`creator_identity_test.go`)
- [x] `live.schema.ts` — permissionMode, creatorPersonId (kept claimMode for now, removed in Phase 3b)
- [x] `session.schema.ts` — mirror fields on local Session
- [x] `liveApi.ts` — createLiveSession gains optional permissionMode/creatorPersonId params
- [x] Fixed 5 TS call sites (sessionStore.ts, toSessionStore.ts x2, sessionStore.test.ts x2) for new required Session fields
- [x] `go test ./...` and `npm test` both green; `tsc --noEmit` clean

## Phase 1 — People UI relocation, Go-Live reachability, share-link polish (Reqs 1, 2, 9) ✅ done 2026-08-16
- [x] Extracted `PeopleListShared.tsx` (PersonInputForm/PeopleList) from PeopleInput.tsx
- [x] `PeopleSection.tsx` (new, session-scoped, bound to sessionStore)
- [x] `SessionHomePage.tsx` renders PeopleSection above the bill list
- [x] `SessionsListPage.tsx` — "Go Live"/"Live" button per session card, navigates to session home with GoLiveSection auto-expanded (via router location state)
- [x] `GoLiveSection.tsx` — copy-button + read-only input for share link, `autoExpand` prop
- [x] `tsc --noEmit`, `npm test`, `npm run build` all green
- [ ] e2e: people editing from session home, copy-link button (deferred — no e2e added yet, existing wizard-step-1 e2e still covers people mutation logic since PeopleInput.tsx still works via shared components)

## Phase 2 — Fix Go-Live activation bug for pre-existing bills (Req 5) ✅ done 2026-08-16
- [x] `syncExistingBillsLive` in sessionStore.ts — fetches server bills, pushes only missing/changed bills+items, called from markSessionLive
- [x] Jest unit test (`sessionStore.goLiveSync.test.ts`, mocks liveApi.ts via jest.mock since it can't be imported directly under Babel/Jest)
- [x] e2e: `go-live-activates-existing-bills.spec.ts` — passes against real Go server
- [x] Fixed 5 existing e2e specs broken by Phase 1's PeopleSection/share-link-input changes (ambiguous "Bob"/"Nina"/"Eli"/"Alice" text matches, `ul li` count, plain-text join-link assertion) — added `data-testid="bill-list"` to the bill `<ul>` for a stable scoped selector
- [x] Full e2e suite (39 tests), `go test ./...`, `npm test` (144 tests) all green

## Phase 3a — Remove claims (backend) + permission-gated mutations + creator identity ✅ done 2026-08-16
- [x] router.go — removed approve/reject/pending claim routes (claim/unclaim routes kept — still the direct-selection mechanism, just no longer approval-gated)
- [x] bill_handlers.go — removed ApproveClaim/RejectClaim/ListPendingClaims; ClaimItem simplified to always direct-write (no more claim_mode branch); requireEditPermission gate added to AddBill/AddItem/UpdateBill/UpdateItem/ClaimItem/UnclaimItem
- [x] session_handlers.go — new `requireEditPermission` helper (blocks joiner-originated requests when permission_mode is read_only; creator's token-free edits always allowed)
- [x] activity_handlers.go — no change needed (ListItemActivity unaffected)
- [x] sse/hub.go — doc comment updated, dropped claim.pending/approved/rejected kinds
- [x] migration 0006_remove_claims.sql — drops item_claims only (item_allocations is canonical consumedBy storage, kept)
- [x] store.go/models.go — removed CreatePendingClaim/CancelPendingClaim/ApproveClaim/ListPendingClaims/RejectClaim, ItemClaim/ClaimStatus types
- [x] Go tests: deleted claim_approval_test.go (obsolete), removed TestUnclaimCancelsPendingClaim, rewrote TestFullJoinApproveClaimFlow → TestFullJoinDirectEditFlow, updated db_test.go's table list, added permission_mode_test.go (read_only rejects joiners, edit allows, creator always allowed)
- [x] `go build`, `go vet`, `go test ./...` all green

## Phase 3b — Remove claims (frontend) + e2e rework ✅ done 2026-08-16
- [x] Deleted ClaimApprovalPage.tsx + route
- [x] liveApi.ts — removed getPendingClaims/approveClaim/rejectClaim, simplified claimItem's response type
- [x] JoinerItemRow.tsx — removed pending-claim tracking entirely (claim takes effect immediately)
- [x] LiveSessionPanel.tsx — removed claimMode state + "Pending claims" link
- [x] ItemAssignment.tsx — checked, no changes needed (its "claim" comments refer to item-share selection, not the removed approval workflow)
- [x] live.schema.ts — removed ClaimItemResponseSchema/PendingClaimSchema, dropped claimMode field, activity action enum now claim|unclaim only
- [x] GoLiveSection.tsx — "Item claims" select replaced with "Joiner permissions" (edit/read_only) + "Which person are you?" creator-identity picker (existing person or add new)
- [x] JoinerSessionView.tsx — now also disables controls when permissionMode === read_only, not just isSettled
- [x] Join UI — no changes needed, existing LiveApiError message surfacing already covers the new server-side rejections
- [x] e2e: deleted claim-approval-page.spec.ts, fixed 8 specs with stale select labels/pending-state assertions, added 5 new specs (joiner-edit-permission, joiner-read-only-permission, creator-claims-own-identity, joiner-cannot-claim-creator-identity, joiner-cannot-reclaim-active-identity)
- [x] Full verification: `tsc --noEmit`, `npm test` (142 tests), `go test ./...`, `npm run build`, full e2e suite (43/43) all green

## Phase 4 — Presence heartbeat wiring + online indicator (Req 3) ✅ done 2026-08-16
- [x] presence_handlers.go — POST heartbeat (joiner-authed), GET presence (public)
- [x] main.go — background sweeper goroutine (presence.FlushAfter interval)
- [x] liveApi.ts — sendPresenceHeartbeat, getPresence
- [x] usePresenceHeartbeat.ts hook, wired into JoinerSessionView.tsx
- [x] PeopleListShared.tsx — PresenceDot + presenceFor prop threading
- [x] PeopleSection.tsx — usePeoplePresence polls presence + joiners list every 500ms while live
- [x] Go test: presence_test.go (heartbeat auth, get presence)
- [x] e2e: presence-indicator.spec.ts — online dot appears, flips offline once joiner tab closes
- [x] Full verification: go test, tsc, jest, full e2e suite (44/44) all green

## Phase 5 — Convert bill wizard to real routes (Req 14) ✅ done 2026-08-16
- [x] App.tsx — `/step/:step` routes, bare `/bill/:billId` redirects via `BillEditorStepRedirect`
- [x] BillEditorPage.tsx — renderStep() now driven by URL step param
- [x] Kept billStore.ts's `step` field (StepIndicator/nextStep/prevStep/goToStep untouched) but two-way synced against the URL, guarded by a ref to prevent the two sync directions re-triggering each other (found via a failing browser-back e2e test — fixed)
- [x] e2e: bill-wizard-browser-back.spec.ts (new); fixed 7 existing specs whose `bill/[^/]+$` URL-wait patterns no longer matched (now `.../step/1$`)
- [x] Full verification: tsc, jest (142), go test, full e2e suite (45/45), build all green

## Phase 6 — Joiner step-wizard UI (Req 4) ✅ done 2026-08-16
- [x] App.tsx — `/join/:code/bills/:billId/step/:step` route
- [x] JoinerBillEditorPage.tsx (new) — 4-step wizard (People/Items/Assign/Summary), independently resolves joiner identity from joinerStorage.ts, reuses JoinerItemRow/AddItemForm (not the creator's billStore-coupled step components, which aren't data-source-agnostic — documented deviation from the plan's literal "fold into ItemsInput/ItemAssignment" suggestion)
- [x] JoinerBillList.tsx — now a plain bill-title picker (mirrors SessionHomePage's bill list), navigates to the step route on click
- [x] JoinerSessionView.tsx simplified (items/claim UI moved out; keeps JoinerSettlementSummary, the cross-bill view)
- [x] e2e: joiner-step-wizard.spec.ts, joiner-step-wizard-readonly.spec.ts (new); updated 13 existing specs whose flows assumed the old inline all-bills view
- [x] Full verification: tsc, jest (142), go test, full e2e suite (47/47), build all green

## Phase 7 — UI polish batch (Reqs 10, 11, 12, 13, 15, 16) ✅ done 2026-08-16
- [x] Dropdown component (ui/components.tsx) — replaced all 7 native `<select>` usages (GoLiveSection x3, Settings, BillSummary, EditItemModal, SplitTypeDrawer, joiner/AddItemForm, JoinPage)
- [x] BillSummary.tsx — "Who Paid?" moved above item breakdown; new "Split Breakdown" section
- [x] splitSummary.ts (`generateSplitSummaryText`) — new section rather than PersonItemRow (which only has per-person, not cross-person, data) — documented deviation
- [x] ActivityLogPage.tsx — person/action Dropdown filters + formatRelativeTime.ts (no new dependency)
- [x] SessionSettlementPage.tsx — new "Bills" section (title/date/total) + receipt-image Modal viewer
- [x] Delete live session — DELETE /api/sessions/{code} (reuses store.PurgeSessionByID), confirm-gated button in GoLiveSection, sessionStore.unmarkSessionLive
- [x] e2e: bill-summary-order, activity-log-filters, settlement-bills-list, delete-live-session (new); Go test delete_session_test.go
- [x] Full verification: tsc, jest (155), go test, full e2e suite (51/51), build all green

## All 8 phases complete (2026-08-16)
Every phase landed as its own commit on `feature/v3Major`, each independently verified (tsc, jest, go test, full e2e suite, production build) before moving to the next. See git log for the phase-by-phase commit messages with full rationale.
