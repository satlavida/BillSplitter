# Claim-approval page + creator live-refresh fix + multi-people/multi-bill e2e coverage

Progress log for the plan in `.claude/plans/few-things-before-this-indexed-kernighan.md`. Each phase is committed separately.

- [x] **Phase 1** — Fix: creator's bill editor live-updates while open (`syncItemsFromLive` billStore action + `connectLiveSync` in `BillEditorPage.tsx`) — commit `5dd0d0d`
- [x] **Phase 2** — Server: list + reject pending claims (migration 0004, `ListPendingClaims`/`RejectClaim` store methods, `GET .../claims/pending` + `POST .../claims/{id}/reject` routes, Go tests) — commit `022cbe4`
- [x] **Phase 3** — Frontend: Claim Approval page (`ClaimApprovalPage.tsx`, `liveApi.ts`/`live.schema.ts` additions, link from `LiveSessionPanel` gated on `claimMode`) — commit `cda94bb`
- [x] **Phase 4** — E2E: multiple claimers, creator sees the output live (`creator-sees-multiple-claimers.spec.ts`) — commit `717cdcb` (also required two small fix commits: `5dd0d0d`, `62a4e5e`)
- [x] **Phase 5** — E2E: multiple bills live-syncing independently (`multi-bill-live-sync.spec.ts`) — commit `e52f523`
- [x] **Phase 6** — E2E: claim-approval page end-to-end, approve one + reject another (`claim-approval-page.spec.ts`) — commit `04bb869` (also fixed a real bug: rejected claims left joiners stuck on "Pending" forever, and the activity log couldn't parse "reject" entries)
- [x] **Phase 7** — E2E: disapprove-joiner flow (`joiner-disapprove.spec.ts`) — commit `ad65af2`
- [x] **Phase 8** — E2E: fraction-correctness badge renders for the creator (`creator-fraction-correctness-badge.spec.ts`) — commit `ae6b25a` (also fixed a real bug: the badge was hidden until the first claim, so it never showed the "0 claimed" warning it exists for)

## All phases complete.

Summary of what shipped: 12 commits total (8 planned phases + 4 small fix
commits discovered while writing the e2e tests — each documented in its own
commit message). Every fix commit was caught by writing the *next* planned
e2e test, not found separately — the tests earned their keep. Full suite
(`go test ./...`, `npm test`, `tsc --noEmit`, `npx playwright test`) green
at every checkpoint.
